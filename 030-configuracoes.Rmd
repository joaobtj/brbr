Configurações {#config}

##  Sumário - TOC (Table Of Contents)

### Desdobramento

É possivel controlar se o sumário será mostrado até as seções/subseções por padrão.

No arquivo `_output.yml`, na seção config -> toc -> collapse

São aceitos os seguintes argumentos: `subsection`, `section` ou `none`
